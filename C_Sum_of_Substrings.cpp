#include<bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

int main(){
    fast
    int T;
    cin>>T;
    while(T--)
    {
        int n,k;
        cin>>n>>k;
        string s;
        cin>>s;
        for(int i=n-1;i>0;i--)
        {
            if(s[i]==0 && s[i-1]==1)
            {

            }
        }
        cout<<endl;
    }
    return 0;
}