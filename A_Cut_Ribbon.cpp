#include<bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define ll long long int

int main(){
    fast
    ll n,a[3],count=0;
    cin>>n>>a[0]>>a[1]>>a[2];
    sort(a,a+3);
    if(n%a[0]==0)
    {
        cout<<n/a[0];
    }
    else{
        if(n)
    }
    return 0;
}